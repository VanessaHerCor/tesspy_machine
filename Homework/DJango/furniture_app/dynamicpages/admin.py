from django.contrib import admin

# Nota: MongoEngine usa documentos, no modelos Django estándar
# El admin de Django no es totalmente compatible con MongoEngine
# Se recomienda usar la API REST para administrar los datos

